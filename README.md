# Simple short URL checker/expander

* Works with:
* * a text file, containing a single URL per line
* * a `.eml` file
* Checks bit.ly, tinyurl.com and t.co URLs
* Run:
```
pipenev shell
python check.py <path_to_file>
```
